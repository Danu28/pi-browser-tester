{
  "launch": { "url": "https://opensource-demo.orangehrmlive.com/web/index.php/auth/login" },
  "steps": [
    { "op": "wait", "selector": "input[name='username']", "timeout": 10000 },
    { "op": "extract", "auto": true },
    {
      "op": "jev",
      "questions": {
        "pageType": { "type": "choice", "criteria": { "login": "OrangeHRM login page with username and password fields and Login button", "dashboard": "OrangeHRM dashboard after login with PIM, Leave, Time modules visible", "error": "Login error page showing invalid credentials or required field message" } },
        "readiness": { "type": "score", "criteria": ["No login form present", "Form present but missing fields or disabled", "Form ready to fill - username and password inputs visible", "Fully ready - form fillable and Login button enabled"] },
        "isLogin": { "type": "noul", "criteria": "This is the OrangeHRM login page and the form is ready to be filled" }
      }
    },
    { "op": "fillForm", "fields": { "input[name='username']": "Admin", "input[name='password']": "admin123" } },
    { "op": "click", "selector": "button[type='submit']" },
    { "op": "wait", "text": "Dashboard", "timeout": 12000 },
    {
      "op": "jev",
      "questions": {
        "loggedIn": { "type": "noul", "criteria": "User is logged in - dashboard, PIM or employee management is visible and login form is gone" },
        "postPage": { "type": "choice", "criteria": { "dashboard": "Dashboard/PIM after successful login", "login": "Still on login page - not logged in", "error": "Error or invalid credentials message shown" } }
      }
    },
    { "op": "extract", "selectors": { "body": "body" }, "aria": true }
  ]
}
